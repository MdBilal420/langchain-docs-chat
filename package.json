{
  "name": "monorepo-template",
  "private": true,
  "version": "1.0.0",
  "author": "Brace Sproul",
  "license": "MIT",
  "packageManager": "yarn@1.22.19",
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "start:api": "yarn turbo run start --filter=api",
    "start:web": "yarn turbo run start --filter=web",
    "build": "yarn turbo run build",
    "lint": "yarn turbo run lint",
    "format": "yarn turbo run format",
    "dev": "yarn turbo run dev"
  },
  "devDependencies": {
    "supabase": "^1.207.9",
    "turbo": "^2.1.3"
  }
}
